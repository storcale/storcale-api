info:
  name: events
  type: http

http:
  method: GET
  url: http://localhost:9902/api/tniv/sheets/events
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
